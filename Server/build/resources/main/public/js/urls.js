// var form = document.getElementById('submit_add_url');

// Send a message when the form is submitted.

    // e.preventDefault();
    // url = document.getElementById("url").value;
    //
    // console.log(distance);

// fetch('/',{
//         method: 'Update',
// })
//     .then(res => $( "#need_refresh" ).load(window.location.href + " #need_refresh" ))
    // .then(res => window.location.reload = window.location.reload(true))
    // .then(res => parent.document.getElementById("need_refresh"))
// function updateDiv()
// {
//     $( "#here" ).load(window.location.href + " #here" );
// }